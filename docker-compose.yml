version: '3.8'

services:
  agent-web:
    build: .
    container_name: unit-test-agent-web
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
